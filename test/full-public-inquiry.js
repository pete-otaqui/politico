var chai = require('chai');
var should = chai.should();

describe('Politico', function() {

    it('should take a series of promise-returning functions, and return a promise');

    it('should deliver the results of one promise-returning function in the series to the next');

    it('should optionally return an error early');

    it('should optionally continue through errors to the end');

    it('should provide each function in the series with all previous return values');
    
});